var array = [12,41];

function updateArray (Arr) {
    Arr = [12,41,50];
    Arr.push(40);
    //Type "node debug" + file name in conmmand line to debug
    //Type cont to continue
    //Type repl to get into code
    debugger;
};

updateArray(array);
console.log(array);